package clase5;

public class clase_5 {

    // clases: representacion del mundo real, entidad. molde de los objetos.
    // objeto: representacion de la realidad, algo especifico

    // atributos van cambiando de valor. el valor que le asigno no queda de forma permamnete sino que puede ser modificado.


    // conceptos importantes: estado del objeto (atributos con su valor en un momento determinado),
    // identidad del objeto(nombre del objeto), comportamiento del objeto (metodos o funciones).

    // la clase se importa solo cuando esta en diferente pakete.

    // cuando un atributo o metodo es privado no podes acceder ni modificar al dato. quien tiene acceso es la clase.

    // get obtengo valor -- simpre para imprimir en pantalla (diempre tenes que setearlo primero)
    // set modifico valor -- para setear valor

    // constantes - valores iguales para todos los objetos. todas las constantes se escriben con mayuscula. si estan acopladas a los objetos.

    public final double IVA = 21.0; // asociada a todos los objetos

    // metodo estatico: se aplican por igual a todos los objetos
    // accedo a el con la clase. atributos y metodos estaticos estan acoplados a la clase, no a los objetos.
   // se subrayan en el diagrama
    public static double desceuntoPorcrntualPorPagoEfectivo = 15.0;

    // siempre los atributos privados.. encapsulados

    // alt mayus punto para hacer zoom


}
